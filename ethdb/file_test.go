package ethdb_test

import (
	"bytes"
	"encoding/binary"
	"io/ioutil"
	"math"
	"math/rand"
	"os"
	"reflect"
	"testing"
	"testing/quick"

	"github.com/gochain-io/gochain/ethdb"
)

func TestFile_Get(t *testing.T) {
	t.Run("OK", func(t *testing.T) {
		path := MustTempFile()
		defer os.Remove(path)

		// Encode key/values to file.
		enc := ethdb.NewFileEncoder(path)
		if err := enc.Open(); err != nil {
			t.Fatal(err)
		} else if err := enc.EncodeKeyValue([]byte("foo"), []byte("bar")); err != nil {
			t.Fatal(err)
		} else if err := enc.Flush(); err != nil {
			t.Fatal(err)
		} else if err := enc.Close(); err != nil {
			t.Fatal(err)
		}

		// Open as file.
		f := ethdb.NewFile(path)
		if err := f.Open(); err != nil {
			t.Fatal(err)
		}
		defer f.Close()

		// Fetch existing keys.
		if v := f.Get([]byte("foo")); string(v) != "bar" {
			t.Fatalf("unexpected value: %q", v)
		}

		// Fetch unknown key.
		if v := f.Get([]byte("no_such_key")); v != nil {
			t.Fatalf("expected nil value, got %q", v)
		}

		// Close file.
		if err := f.Close(); err != nil {
			t.Fatal(err)
		}
	})
}

// Ensure ethdb.File can fetch keys using randomized test data.
func TestFile_Quick(t *testing.T) {
	if testing.Short() {
		t.Skip("short")
	}

	const maxCount = 10000
	const maxKeyLen = 2000
	const maxValueLen = 10000

	quick.Check(func(keys, values [][]byte) bool {
		path := MustTempFile()
		defer os.Remove(path)

		// Write data to file.
		if err := EncodeToFile(path, keys, values); err != nil {
			t.Fatal(err)
		}

		// Open as file.
		f := ethdb.NewFile(path)
		if err := f.Open(); err != nil {
			t.Fatal(err)
		}
		defer f.Close()

		// Verify all key/value pairs exist.
		for i := range keys {
			if v := f.Get(keys[i]); !bytes.Equal(values[i], v) {
				t.Fatalf("value mismatch: (key=%q) expected %q, got %q", keys[i], values[i], v)
			}
		}

		// Verify we can iterate them in order.
		itr, i := f.Iterator(), 0
		for ; itr.Next(); i++ {
			if !bytes.Equal(itr.Key(), keys[i]) {
				t.Fatalf("iterator key mismatch:\nexpected %x\ngot %x", keys[i], itr.Key())
			} else if !bytes.Equal(itr.Value(), values[i]) {
				t.Fatal("iterator value mismatch")
			}
		}
		if i != len(keys) {
			t.Fatal("short iterator")
		}

		return true
	}, &quick.Config{
		MaxCount: 10,
		Values: func(args []reflect.Value, rand *rand.Rand) {
			n := rand.Intn(maxCount-1) + 1
			args[0] = reflect.ValueOf(generateKeys(n, 1, maxKeyLen-1, rand))
			args[1] = reflect.ValueOf(generateValues(n, 0, maxValueLen, rand))
		},
	})
}

func BenchmarkFile_Get(b *testing.B) {
	path := MustTempFile()
	defer os.Remove(path)

	// Encode key/value pairs.
	const n = 100000
	keys, values := make([][]byte, n), make([][]byte, n)
	for i := 0; i < n; i++ {
		keys[i] = make([]byte, 32)
		binary.BigEndian.PutUint64(keys[i], uint64(i))
		values[i] = make([]byte, 1024)
	}
	EncodeToFile(path, keys, values)

	// Determine random access pattern.
	perm := rand.Perm(n)

	// Open as file.
	f := ethdb.NewFile(path)
	if err := f.Open(); err != nil {
		b.Fatal(err)
	}
	defer f.Close()

	b.ResetTimer()
	b.ReportAllocs()

	// Lookup key/value pairs.
	for i := 0; i < b.N; i++ {
		key := keys[perm[i%len(perm)]]
		if v := f.Get(key); v == nil {
			b.Fatal("key not found")
		}
	}
}

func MustTempFile() string {
	f, err := ioutil.TempFile("", "gochain-file-")
	if err != nil {
		panic(err)
	}
	f.Close()
	return f.Name()
}

// EncodeToFile encodes a set of key/value pairs to an ethdb.File at path.
func EncodeToFile(path string, keys, values [][]byte) error {
	// Build file.
	enc := ethdb.NewFileEncoder(path)
	if err := enc.Open(); err != nil {
		return err
	}
	defer enc.Close()

	// Write all keys.
	for i := range keys {
		if err := enc.EncodeKeyValue(keys[i], values[i]); err != nil {
			return err
		}
	}

	// Flush all data.
	if err := enc.Flush(); err != nil {
		return err
	} else if err := enc.Close(); err != nil {
		return err
	}
	return nil
}

// generateKeys returns a set of n unique, randomly generated keys.
func generateKeys(n, min, max int, rand *rand.Rand) [][]byte {
	a := make([][]byte, n)
	for i, m := 0, make(map[string]struct{}); i < len(a); i++ {
		a[i] = generateBytes(min, max, rand)
		if _, ok := m[string(a[i])]; ok {
			i--
			continue
		}
		m[string(a[i])] = struct{}{}
	}
	return a
}

// generateValues returns a set of n randomly generated values.
func generateValues(n, min, max int, rand *rand.Rand) [][]byte {
	a := make([][]byte, n)
	for i := range a {
		a[i] = generateBytes(min, max, rand)
	}
	return a
}

// generateBytes returns a randomly generated byte slice between min & max length.
func generateBytes(min, max int, rand *rand.Rand) []byte {
	b := make([]byte, rand.Intn(max-min)+min)
	for i := range b {
		b[i] = byte(rand.Intn(math.MaxInt8))
	}
	return b
}
